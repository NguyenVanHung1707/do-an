% !TeX root = ../DoAn.tex
\documentclass[../DoAn.tex]{subfiles}
\begin{document}

Tiếp nối các phân tích yêu cầu tại Chương 2 và nền tảng công nghệ ở Chương 3, Chương 4 trình bày thiết kế chi tiết, quá trình xây dựng, triển khai và đánh giá thực nghiệm hệ thống phần mềm tối ưu hóa quy trình điểm danh và quản lý học sinh. Nội dung chương tập trung vào việc mô tả thiết kế kiến trúc tổng thể của các phân hệ (\path{FE_WEB}, \path{frontend_student}, \path{frontend_teacher}, \path{backend-core}, \path{detect-face}, \path{ai-proctor}), thiết kế cơ sở dữ liệu PostgreSQL \cite{postgresql_docs2026}, xây dựng các kịch bản kiểm thử chức năng, và cấu hình triển khai thực tế bằng Docker Compose \cite{docker_compose_docs2026} kết hợp NGINX reverse proxy \cite{nginx_docs2026}.

\section{Thiết kế kiến trúc}
\label{section:4.1}
\subsection{Lựa chọn kiến trúc phần mềm}
\label{subsection:4.1.1}
Đối với hệ thống quản lý học tập tích hợp điểm danh Face ID và giám sát thi trực tuyến, đồ án lựa chọn kiến trúc phần mềm lai, trong đó phần nghiệp vụ trung tâm được tổ chức theo kiến trúc phân tầng ba lớp, còn các chức năng AI có tải xử lý riêng được tách thành các dịch vụ độc lập theo định hướng SOA/microservice nhẹ \cite{newman2021buildingmicroservices}. Cách lựa chọn này phù hợp với phạm vi đồ án vì hệ thống vừa cần giữ dữ liệu đào tạo nhất quán trong một backend chính, vừa cần tách các tác vụ xử lý ảnh và giám sát webcam ra khỏi luồng nghiệp vụ thông thường.

Về lý thuyết, kiến trúc phân tầng ba lớp chia hệ thống thành tầng giao diện, tầng xử lý nghiệp vụ và tầng dữ liệu. Khi áp dụng vào đồ án, các tầng này được ánh xạ thành những thành phần cụ thể như sau:
\begin{itemize}
    \item \textbf{Tầng giao diện}: gồm \path{FE_WEB} cho người quản trị, giảng viên và học viên trên trình duyệt; \path{frontend_student} cho học viên trên thiết bị di động; và \path{frontend_teacher} cho giảng viên trên thiết bị di động. Các client này không truy cập trực tiếp cơ sở dữ liệu mà gửi yêu cầu thông qua API do backend cung cấp.
    \item \textbf{Tầng xử lý nghiệp vụ}: là phân hệ \path{backend-core} được xây dựng bằng Spring Boot \cite{spring_boot_docs2026}. Bên trong backend, các gói \path{controller}, \path{service}, \path{repository}, \path{entity}, \path{dto} và \path{config} lần lượt đảm nhiệm vai trò tiếp nhận yêu cầu, xử lý nghiệp vụ, truy xuất dữ liệu, biểu diễn mô hình dữ liệu, trao đổi dữ liệu qua API và cấu hình hệ thống.
    \item \textbf{Tầng dữ liệu}: sử dụng PostgreSQL để lưu trữ các thực thể như người dùng, lớp học phần, bài thi, kết quả điểm danh, bài nộp và nhật ký vi phạm \cite{postgresql_docs2026}.
\end{itemize}

So với kiến trúc ba lớp thuần túy, hệ thống có bổ sung các dịch vụ chuyên biệt để xử lý nghiệp vụ AI:
\begin{itemize}
    \item \textbf{\path{detect-face}}: đảm nhiệm nhận diện khuôn mặt phục vụ điểm danh nhóm.
    \item \textbf{\path{ai-proctor}}: đảm nhiệm giám sát thi trực tuyến thông qua luồng webcam và WebSocket \cite{fastapi_docs2026,fette2011websocket}.
    \item \textbf{\path{backend-core}}: giữ vai trò điều phối, nhận yêu cầu từ client, kiểm tra điều kiện nghiệp vụ, gọi dịch vụ AI khi cần và lưu kết quả cuối cùng vào cơ sở dữ liệu.
\end{itemize}
Hai dịch vụ AI được triển khai độc lập với backend nghiệp vụ, giúp \path{backend-core} không phải mang theo các phụ thuộc nặng của xử lý ảnh, đồng thời cho phép cấu hình, thay thế hoặc mở rộng riêng khi tải xử lý AI tăng lên.

Như vậy, kiến trúc cụ thể của hệ thống có thể được tóm tắt theo ba nhóm thành phần chính:
\begin{itemize}
    \item \textbf{Nhóm client}: Web và ứng dụng di động.
    \item \textbf{Nhóm backend nghiệp vụ}: controller, service, repository, entity, DTO và cấu hình bảo mật.
    \item \textbf{Nhóm dịch vụ hạ tầng và AI}: PostgreSQL, Keycloak, \path{detect-face}, \path{ai-proctor}, NGINX và Docker Compose \cite{keycloak2026,nginx_docs2026,docker_compose_docs2026}.
\end{itemize}
Cách tổ chức này giữ được ưu điểm dễ phát triển, dễ kiểm soát dữ liệu của kiến trúc phân tầng, đồng thời bổ sung khả năng tách biệt và mở rộng cho các tác vụ AI đặc thù của đề tài.

\subsection{Thiết kế tổng quan}
\label{subsection:4.1.2}

Thiết kế tổng quan của hệ thống được tổ chức theo mô hình kiến trúc phân tầng, trong đó các thành phần được chia theo vai trò giao diện, xử lý nghiệp vụ, lưu trữ dữ liệu và dịch vụ AI chuyên biệt. Cách phân chia này giúp hệ thống dễ bảo trì, hạn chế phụ thuộc chéo và phù hợp với kiến trúc lai đã lựa chọn ở Mục \ref{subsection:4.1.1}. Các nguyên tắc thiết kế chính gồm:
\begin{itemize}
    \item \textbf{Phụ thuộc một chiều}: Các client chỉ gọi API; controller gọi service; service điều phối repository hoặc dịch vụ ngoài; tầng dữ liệu không phụ thuộc ngược lên tầng trên.
    \item \textbf{Không phụ thuộc vòng}: Các gói được tách theo trách nhiệm rõ ràng, tránh việc các module gọi qua lại lẫn nhau.
    \item \textbf{Tách riêng tác vụ AI}: Các chức năng nhận diện khuôn mặt và giám sát thi được triển khai độc lập để giảm tải cho backend nghiệp vụ.
\end{itemize}

Sơ đồ gói thiết kế tổng quan của hệ thống được trình bày trong Hình \ref{fig:BieuDoGoiThietKeTongQuan}.

\FloatBarrier
\begin{center}
    \centering
    \includegraphics[width=0.95\textwidth]{Hinhve/BieuDoGoiThietKeTongQuan.png}
    \captionof{figure}{Biểu đồ gói thiết kế tổng quan}
    \label{fig:BieuDoGoiThietKeTongQuan}
\end{center}
\FloatBarrier

Vai trò các tầng và gói chính được tóm tắt trong Bảng \ref{table:architecture_overview_packages}.

\begin{longtable}{|p{0.22\textwidth}|p{0.28\textwidth}|p{0.40\textwidth}|}
    \caption{Tóm tắt các tầng và gói chính trong thiết kế tổng quan}
    \label{table:architecture_overview_packages}\\
        \hline
        \textbf{Tầng/nhóm thành phần} & \textbf{Gói hoặc phân hệ chính} & \textbf{Vai trò} \\ \hline
        Giao diện người dùng & \path{FE_WEB}, \path{frontend_student}, \path{frontend_teacher} & Cung cấp giao diện Web và ứng dụng di động cho quản trị viên, giảng viên và học viên; gửi yêu cầu đến backend thông qua API. \\ \hline
        Nghiệp vụ và API & \path{controller}, \path{service}, \path{dto} & Tiếp nhận yêu cầu, kiểm tra dữ liệu đầu vào, xử lý nghiệp vụ điểm danh, thi trực tuyến, lớp học phần và trả phản hồi cho client. \\ \hline
        Truy xuất và mô hình dữ liệu & \path{repository}, \path{entity} & Ánh xạ dữ liệu nghiệp vụ sang các bảng PostgreSQL và thực hiện các thao tác truy xuất dữ liệu qua Spring Data JPA \cite{spring_boot_docs2026,postgresql_docs2026}. \\ \hline
        Hạ tầng dùng chung & \path{config}, \path{exception}, \path{util} & Cấu hình bảo mật, Keycloak SSO, CORS, WebSocket, xử lý ngoại lệ và các tiện ích dùng chung như JWT, GPS, file. \\ \hline
        Dịch vụ AI & \path{detect-face}, \path{ai-proctor} & Xử lý nhận diện khuôn mặt nhóm bằng RetinaFace/ArcFace và giám sát thi trực tuyến bằng webcam, WebSocket, MediaPipe FaceMesh và solvePnP \cite{Deng_2020_CVPR,Deng_2019_CVPR,Lugaresi2019MediaPipe,Lepetit2009EPnP}. \\ \hline
\end{longtable}

Nhìn chung, thiết kế tổng quan giữ backend Spring Boot làm trung tâm điều phối nghiệp vụ, PostgreSQL làm nơi lưu trữ dữ liệu chính, còn các tác vụ AI được tách thành dịch vụ độc lập. Cách tổ chức này giúp hệ thống duy trì ranh giới trách nhiệm rõ ràng giữa các tầng, đồng thời vẫn hỗ trợ mở rộng riêng các thành phần có tải xử lý cao.
\subsection{Thiết kế chi tiết gói}
\label{subsection:4.1.3}

Phần này trình bày thiết kế chi tiết gói cho hai phân hệ trọng tâm: \textbf{Quản lý thi trực tuyến \& Giám sát AI} và \textbf{Điểm danh thông minh FaceID nhóm \& Định vị GPS}. Mỗi phân hệ được mô tả bằng một sơ đồ gói và bảng tóm tắt vai trò các thành phần chính.

\subsubsection{Phân hệ Quản lý thi trực tuyến \& Giám sát phòng thi bằng trí tuệ nhân tạo (Assessment \& AI Proctoring)}
\label{subsubsection:4.1.3_assessment}

Phân hệ \textbf{Quản lý thi trực tuyến \& Giám sát AI (Assessment \& AI Proctoring)} đảm nhiệm các nghiệp vụ liên quan đến tổ chức bài kiểm tra, quản lý câu hỏi, ghi nhận bài làm và hỗ trợ chấm điểm. Bên cạnh đó, phân hệ còn tích hợp các thành phần giám sát bằng trí tuệ nhân tạo nhằm phát hiện những dấu hiệu bất thường trong quá trình làm bài, từ đó tăng tính minh bạch và độ tin cậy của hoạt động thi trực tuyến.

Chi tiết về cấu trúc các lớp (classes), giao diện (interfaces) và mối quan hệ giữa chúng trong phân hệ Quản lý thi \& Giám sát AI được thể hiện trong Hình \ref{fig:BieuDoThietKeChiTietGoi}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{Hinhve/BieuDoThietKeChiTietGoi_QuanLyThi_GiamSatAI.png}
    \caption{Biểu đồ thiết kế chi tiết gói - Quản lý thi \& Giám sát AI}
    \label{fig:BieuDoThietKeChiTietGoi}
\end{figure}
\FloatBarrier

Sơ đồ trong Hình \ref{fig:BieuDoThietKeChiTietGoi} được tổ chức theo kiến trúc Spring Boot \cite{spring_boot_docs2026}, trong đó các gói chính được tóm tắt ở Bảng \ref{table:assessment_package_summary}.

\begin{longtable}{|p{0.22\textwidth}|p{0.68\textwidth}|}
    \caption{Tóm tắt các gói chính của phân hệ Quản lý thi \& Giám sát AI}
    \label{table:assessment_package_summary}\\
    \hline
    \textbf{Gói/thành phần} & \textbf{Vai trò chính} \\ \hline
    \path{controller} & Gồm \path{AssessmentController} và \path{AIProctorController}, tiếp nhận yêu cầu về đề thi, bài làm, cấu hình giám sát và nhật ký vi phạm. \\ \hline
    \path{dto} & Gồm \path{AssessmentDto}, \path{StudentSubmissionDto}, dùng để trao đổi dữ liệu giữa client và backend. \\ \hline
    \path{service}/\path{implement} & Gồm \path{AssessmentService} và \path{AssessmentServiceImplement}, xử lý nghiệp vụ soạn đề, lưu bài, chấm trắc nghiệm, kiểm soát thời gian và kết nối dịch vụ \path{ai-proctor}. \\ \hline
    \path{repository}/\path{entity} & Quản lý các thực thể \path{Assessment}, \path{AssessmentQuestion}, \path{StudentSubmission}, \path{StudentAnswer}, \path{Student} thông qua Spring Data JPA và PostgreSQL \cite{postgresql_docs2026}. \\ \hline
    \path{exception} & Chuẩn hóa lỗi nghiệp vụ như nộp bài quá giờ, truy cập không hợp lệ hoặc vi phạm do AI ghi nhận. \\ \hline
\end{longtable}

\subsubsection{Phân hệ Điểm danh thông minh bằng FaceID nhóm \& Định vị GPS (Smart Attendance)}
\label{subsubsection:4.1.3_attendance}

Bên cạnh phân hệ Quản lý thi, phân hệ \textbf{Điểm danh thông minh bằng FaceID nhóm \& Định vị GPS (Smart Attendance)} hỗ trợ giảng viên ghi nhận trạng thái tham gia học tập của học viên theo từng buổi học bằng cách kết hợp nhận diện khuôn mặt và kiểm tra vị trí GPS. Dữ liệu điểm danh được quản lý trong backend Spring Boot, lưu trữ bằng PostgreSQL \cite{spring_data_jpa_docs2026,postgresql_docs2026}; quá trình nhận diện khuôn mặt sử dụng RetinaFace và ArcFace \cite{Deng_2020_CVPR,Deng_2019_CVPR}; còn việc kiểm tra vị trí dựa trên công thức Haversine \cite{sinnott1984virtues}. Cách tách riêng các bước tiếp nhận dữ liệu, kiểm tra điều kiện và lưu kết quả giúp hệ thống dễ mở rộng, dễ bảo trì và thuận tiện cho việc đối soát khi có sai lệch.

Chi tiết về cấu trúc các lớp (classes), giao diện (interfaces) và mối quan hệ giữa chúng trong phân hệ Điểm danh thông minh được thể hiện trong Hình \ref{fig:BieuDoThietKeChiTietGoi_DiemDanh}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{Hinhve/BieuDoThietKeChiTietGoi_DiemDanhFaceID_GPS.png}
    \caption{Biểu đồ thiết kế chi tiết gói - Điểm danh FaceID nhóm \& Định vị GPS}
    \label{fig:BieuDoThietKeChiTietGoi_DiemDanh}
\end{figure}
\FloatBarrier

Sơ đồ trong Hình \ref{fig:BieuDoThietKeChiTietGoi_DiemDanh} được tóm tắt theo các nhóm thành phần chính ở Bảng \ref{table:attendance_package_summary}.

\begin{longtable}{|p{0.22\textwidth}|p{0.68\textwidth}|}
    \caption{Tóm tắt các gói chính của phân hệ Điểm danh thông minh}
    \label{table:attendance_package_summary}\\
    \hline
    \textbf{Gói/thành phần} & \textbf{Vai trò chính} \\ \hline
    \path{controller} & Gồm \path{TeacherController} và \path{StudentController}, tiếp nhận yêu cầu tạo phiên, gửi GPS, điểm danh và xem kết quả. \\ \hline
    \path{dto} & Gồm \path{FormDto}, \path{StudentAttendancePreviewDto}, \path{ConfirmAttendanceChangesDto}, \path{LocationCheckRequest}, dùng để truyền dữ liệu điểm danh giữa client và backend. \\ \hline
    \path{service}/\path{implement} & Gồm \path{TeacherService}, \path{StudentService} và các lớp hiện thực, xử lý geofencing, chống giả lập GPS, kết nối \path{detect-face} và tổng hợp kết quả điểm danh. \\ \hline
    \path{repository}/\path{entity} & Quản lý \path{Teacher}, \path{Student}, \path{Register}, \path{Form}, \path{FormSubmission}, \path{AttendanceLog} bằng Spring Data JPA và PostgreSQL \cite{spring_data_jpa_docs2026,postgresql_docs2026}. \\ \hline
    \path{exception}/\path{spring-framework} & Chuẩn hóa phản hồi lỗi khi GPS ngoài phạm vi, FaceID không khớp hoặc phát hiện giả lập vị trí. \\ \hline
\end{longtable}

\section{Thiết kế chi tiết}
\subsection{Thiết kế giao diện}
Giao diện hệ thống được thiết kế theo hướng ứng dụng nghiệp vụ, ưu tiên thao tác nhanh, dễ đọc dữ liệu và hạn chế sai sót trong các quy trình như điểm danh, làm bài thi, chấm điểm và quản lý lớp học phần. Phần Web được hiện thực trong phân hệ \path{FE_WEB} bằng React, Vite và TailwindCSS \cite{react_docs2026,vite_docs2026,tailwindcss_docs2026}. Bố cục chính gồm \path{Navbar}, \path{Sidebar} và vùng nội dung trung tâm thay đổi theo vai trò người dùng.

Các chuẩn giao diện chính được tóm tắt trong Bảng \ref{table:ui_design_summary}. Hệ thống hướng tới màn hình máy tính từ $1366 \times 768$ đến $1920 \times 1080$ pixel cho thao tác nghiệp vụ thường xuyên; trên thiết bị nhỏ, \path{Sidebar} chuyển thành menu trượt, nội dung chính giảm khoảng đệm và các bảng/thẻ thông tin chuyển về bố cục một cột.

\begin{longtable}{|p{0.24\textwidth}|p{0.30\textwidth}|p{0.36\textwidth}|}
    \caption{Tóm tắt chuẩn thiết kế giao diện Web}
    \label{table:ui_design_summary}\\
        \hline
        \textbf{Nhóm thiết kế} & \textbf{Thành phần/giá trị sử dụng} & \textbf{Mục đích} \\ \hline
        Bố cục tổng thể & \path{Navbar}, \path{Sidebar}, vùng nội dung chính & Giữ khung thao tác thống nhất, giúp người dùng chuyển giữa các màn hình mà không phải học lại cách sử dụng. \\ \hline
        Hiển thị đáp ứng & Breakpoint và utility class của TailwindCSS & Tối ưu giao diện cho máy tính, máy tính bảng và điện thoại; menu trái có thể thu gọn trên màn hình nhỏ. \\ \hline
        Màu sắc và trạng thái & \path{primary}, \path{success}, \path{warning}, \path{error}, \path{info} & Phân biệt thao tác chính, trạng thái thành công, cảnh báo, lỗi và thông tin hệ thống. \\ \hline
        Kiểu chữ và thẻ nội dung & \path{Plus Jakarta Sans}, \path{Inter}, \path{Fira Code}, component \path{Card} & Tách rõ tiêu đề, nội dung, mã định danh và nhóm thông tin theo từng nhiệm vụ. \\ \hline
        Phản hồi thao tác & Loading, thông báo lỗi/thành công, vô hiệu hóa nút khi đang xử lý & Tránh gửi trùng yêu cầu và giúp người dùng biết trạng thái xử lý của hệ thống. \\ \hline
\end{longtable}

Giao diện được chia theo vai trò đăng nhập, không dùng một menu chung cho tất cả người dùng. Mỗi vai trò chỉ nhìn thấy các chức năng phù hợp với quyền hạn và trạng thái tài khoản của mình. Ví dụ, tài khoản giảng viên chưa được phê duyệt sẽ chuyển sang màn hình \path{PendingApproval}; học viên chưa hoàn thiện hồ sơ sẽ được yêu cầu bổ sung thông tin trước khi dùng các chức năng còn lại.

\begin{longtable}{|p{0.16\textwidth}|p{0.30\textwidth}|p{0.44\textwidth}|}
    \caption{Thiết kế giao diện theo vai trò người dùng}
    \label{table:ui_role_design}\\
        \hline
        \textbf{Vai trò} & \textbf{Màn hình/chức năng chính} & \textbf{Định hướng thiết kế} \\ \hline
        Quản trị viên & \path{AdminDashboard}, \path{TeacherApproval}, \path{SemesterManagement} & Ưu tiên bảng dữ liệu, chỉ số tổng quan, nút phê duyệt/từ chối rõ ràng và trạng thái xử lý minh bạch. \\ \hline
        Giảng viên & \path{ClassManagement}, \path{ClassDetail}, \path{AssessmentManagement}, \path{GradeAssessment}, \path{PhotoAttendance} & Hỗ trợ quản lý lớp học phần, tài liệu, bài kiểm tra, chấm điểm và điểm danh; riêng điểm danh ảnh có bước xem trước trước khi lưu. \\ \hline
        Học viên & \path{MyCourses}, \path{TakeAssessment}, \path{GradesAndAttendance}, \path{Timetable}, \path{FaceUpload} & Giảm số bước thao tác, tập trung vào xem lớp học, tài liệu, lịch học, làm bài thi và tra cứu điểm/chuyên cần. \\ \hline
\end{longtable}

Về tương tác, các thao tác ảnh hưởng đến dữ liệu quan trọng đều có bước kiểm tra trước khi ghi nhận. Với điểm danh bằng ảnh lớp học, hệ thống tạo danh sách xem trước để giảng viên đối soát rồi mới xác nhận lưu. Với bài thi trực tuyến, hệ thống kiểm soát thời gian mở/đóng và chỉ cho phép nộp khi thỏa mãn điều kiện nghiệp vụ. Cách thiết kế này giúp giao diện vừa gọn, vừa phù hợp với đặc thù của hệ thống quản lý học tập tích hợp điểm danh và giám sát AI.
\subsection{Thiết kế lớp}
Thiết kế lớp đặc tả cấu trúc của các thuộc tính và phương thức trong những lớp đối tượng chủ đạo, đồng thời mô tả luồng thông điệp động qua biểu đồ trình tự để giải quyết hai nghiệp vụ cốt lõi: thi trực tuyến giám sát AI và điểm danh FaceID nhóm kết hợp định vị GPS.

\subsubsection{Đặc tả cấu trúc các lớp chính}
Hệ thống sử dụng các lớp cốt lõi được tổ chức theo kiến trúc phân tầng của Spring Boot. Nhóm \texttt{entity} biểu diễn dữ liệu lưu trong PostgreSQL, nhóm \texttt{repository} truy xuất dữ liệu, nhóm \texttt{service}/\texttt{implement} xử lý nghiệp vụ, còn nhóm \texttt{dto} chuẩn hóa dữ liệu trao đổi giữa client và backend. Trong phạm vi đồ án, phần thiết kế lớp tập trung vào hai nghiệp vụ chính: thi trực tuyến có giám sát AI và điểm danh thông minh bằng FaceID/GPS.

\begin{itemize}
    \item \textbf{Lớp thực thể \texttt{Assessment}}: Đại diện cho bài thi, lưu thông tin như tên bài thi, thời gian mở/đóng, danh sách câu hỏi và các cấu hình kiểm soát như \texttt{isLocationRequired}, \texttt{isCameraRequired}, \texttt{allowedRadiusMeters}, \texttt{teacherLatitude}, \texttt{teacherLongitude}. Các thuộc tính này cho phép từng bài thi tự xác định yêu cầu GPS, camera và bán kính làm bài hợp lệ.
    
    \item \textbf{Nhóm lớp bài làm}: Gồm \texttt{StudentSubmission} và \texttt{StudentAnswer}. \texttt{StudentSubmission} lưu một lần làm bài của học viên, bao gồm trạng thái làm bài, thời điểm bắt đầu, thời điểm nộp và tổng điểm; \texttt{StudentAnswer} lưu câu trả lời cụ thể cho từng câu hỏi. Nhóm lớp này giúp tách dữ liệu đề thi khỏi dữ liệu bài làm, thuận tiện cho lưu nháp, nộp bài và chấm điểm tự động.
    
    \item \textbf{Lớp nghiệp vụ \texttt{AssessmentServiceImplement}}: Điều phối nghiệp vụ thi trực tuyến giữa controller, repository và dịch vụ giám sát AI. Các phương thức chính gồm:
    \begin{itemize}
        \item \texttt{startAssessment(assessmentId, studentId, location)}: Khởi tạo bài thi, gọi \texttt{validateAssessmentLocation} để kiểm tra GPS và lưu trạng thái \texttt{IN\_PROGRESS}.
        \item \texttt{submitAssessment(submissionId, studentId, location)}: Xác thực vị trí lần cuối, tự động chấm điểm trắc nghiệm và câu trả lời ngắn qua \texttt{autoGradeMultipleChoice} và \texttt{autoGradeShortAnswer}, cập nhật trạng thái bài thi thành \texttt{GRADED} hoặc \texttt{SUBMITTED}.
        \item \texttt{validateAssessmentLocation(assessment, location)}: Áp dụng công thức Haversine \cite{sinnott1984virtues} để tính khoảng cách địa lý thực tế và quét cờ giả lập vị trí (\texttt{mockLocationDetected}).
    \end{itemize}
    
    \item \textbf{Nhóm lớp điểm danh}: Gồm \texttt{Form}, \texttt{FormSubmission}, \texttt{Register} và \texttt{AttendanceLog}. \texttt{Form} biểu diễn phiên điểm danh, chứa mã OTP, thời gian mở và cấu hình vị trí; \texttt{FormSubmission} lưu từng lượt check-in; \texttt{Register} lưu hồ sơ FaceID gốc; còn \texttt{AttendanceLog} lưu lịch sử chuyên cần sau khi kết quả được xác nhận.
    
    \item \textbf{Lớp nghiệp vụ \texttt{TeacherServiceImplement}}: Thực thi nghiệp vụ điểm danh phía giảng viên, từ tạo phiên điểm danh, nhận kết quả nhận diện từ dịch vụ \texttt{detect-face}, tạo danh sách xem trước đến lưu kết quả chính thức. Các phương thức chính gồm:
    \begin{itemize}
        \item \texttt{createForm(courseId, formDto, sub)}: Tạo phiên điểm danh kèm mã OTP và cấu hình Geofencing.
        \item \texttt{previewAttendanceFace(courseId, lectureNumber, recognizedStudentIds, sub)}: Nhận danh sách ID sinh viên được AI phát hiện từ ảnh tập thể để chuẩn bị bảng xem trước đối sánh.
        \item \texttt{confirmAttendanceChanges(dto, sub)}: Giảng viên duyệt chỉnh sửa và lưu chính thức kết quả điểm danh vào \texttt{AttendanceLog}, đồng thời chạy stored procedure tự động cập nhật số liệu chuyên cần.
    \end{itemize}
    
    \item \textbf{Lớp nghiệp vụ \texttt{StudentServiceImplement}}: Xử lý các thao tác phía học viên như đăng ký hồ sơ khuôn mặt, nhập OTP, gửi tọa độ GPS và check-in cá nhân. Lớp này phối hợp với các repository liên quan để xác minh điều kiện điểm danh trước khi tạo bản ghi hợp lệ.
\end{itemize}

Như vậy, các lớp chính tạo thành một chuỗi xử lý rõ ràng: controller tiếp nhận yêu cầu, DTO chuẩn hóa dữ liệu, service kiểm tra nghiệp vụ, repository thao tác dữ liệu và entity phản ánh trạng thái lưu trữ cuối cùng. Cấu trúc này giúp hệ thống dễ mở rộng thêm điều kiện bảo mật hoặc thuật toán AI mới mà không phải thay đổi toàn bộ luồng xử lý.

\subsubsection{Biểu đồ trình tự mô tả luồng truyền thông điệp}

\paragraph{1. UC-10: Làm bài thi trực tuyến và Giám sát AI}
Biểu đồ trình tự mô tả quy trình học viên làm bài thi trực tuyến dưới sự giám sát liên tục của dịch vụ AI thời gian thực và ghi nhận vi phạm về máy chủ Backend, được minh họa ở Hình \ref{fig:UC10_sequence_diagram}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Hinhve/UC_10_Thi_truc_tuyen_Giam_sat_AI.png}
    \caption{Biểu đồ trình tự UC-10: Làm bài thi trực tuyến và Giám sát AI}
    \label{fig:UC10_sequence_diagram}
\end{figure}
\FloatBarrier

Quy trình tương tác diễn ra như sau:
\begin{enumerate}
    \item \textbf{Khởi tạo thi}: Học viên truy cập trang thi từ client (\texttt{FE\_WEB / App}), ứng dụng gửi yêu cầu lấy đề thi thông qua \texttt{getAssessmentsForCourse}. Khi bắt đầu làm bài, client gọi API \texttt{startAssessment} của \texttt{AssessmentController}. Lớp nghiệp vụ \texttt{AssessmentService} gọi phương thức \texttt{validateAssessmentLocation} để kiểm tra định vị GPS và chống giả lập, sau đó tạo bản ghi bài nộp \texttt{StudentSubmission} với trạng thái \texttt{IN\_PROGRESS} trong PostgreSQL.
    \item \textbf{Giám sát AI trực tuyến}: Client kết nối WebSocket tới dịch vụ AI (\texttt{ai-proctor}) truyền liên tục luồng frame hình ảnh từ webcam. Dịch vụ AI gọi MediaPipe FaceMesh \cite{Lugaresi2019MediaPipe} để tính góc quay đầu và hướng nhìn (sử dụng EPnP \cite{Lepetit2009EPnP} giải PnP). Nếu phát hiện bất thường liên tục quá 3 giây, dịch vụ AI trích xuất video vi phạm độ dài 10 giây lưu vào máy chủ và gửi cảnh báo vi phạm về backend.
    \item \textbf{Nộp bài}: Học viên kết thúc bài làm, ứng dụng gửi yêu cầu \texttt{submitAssessment} kèm GPS. Hệ thống chấm điểm tự động các câu trắc nghiệm và điền khuyết rồi lưu kết quả với trạng thái \texttt{GRADED} hoặc \texttt{SUBMITTED}.
\end{enumerate}

\paragraph{2. UC-06: Điểm danh nhận diện FaceID nhóm và Định vị GPS}
Biểu đồ trình tự mô tả quy trình giảng viên điểm danh nhóm bằng AI FaceID kết hợp với học viên check-in cá nhân bằng định vị GPS phòng học và mã OTP bổ sung, được minh họa ở Hình \ref{fig:UC06_sequence_diagram}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Hinhve/UC_06_Diem_danh_FaceID_nhom_GPS.png}
    \caption{Biểu đồ trình tự UC-06: Điểm danh nhận diện FaceID nhóm và Định vị GPS}
    \label{fig:UC06_sequence_diagram}
\end{figure}
\FloatBarrier

Quy trình tương tác diễn ra như sau:
\begin{enumerate}
    \item \textbf{Điểm danh nhóm FaceID}: Giảng viên gửi ảnh lớp học tới AI Service (\texttt{detect-face}). Dịch vụ sử dụng RetinaFace \cite{Deng_2020_CVPR} để phát hiện khuôn mặt và đối sánh đặc trưng qua ArcFace \cite{Deng_2019_CVPR} để tìm ID học viên. Kết quả nhận diện được gửi về \texttt{TeacherController} để tạo danh sách xem trước. Giảng viên đối soát và lưu chính thức kết quả qua \texttt{confirmAttendanceChanges}, hệ thống tự động chạy stored procedure cập nhật tổng số buổi đi học của học viên.
    \item \textbf{Check-in cá nhân}: Giảng viên phát mã OTP trên màn chiếu. Học viên nhập mã OTP, chụp ảnh selfie xác thực khuôn mặt (qua ArcFace) và gửi định vị GPS. Lớp nghiệp vụ \texttt{StudentService} xác thực định vị bằng Geofencing phòng học và kiểm tra cấm giả lập GPS, sau đó lưu kết quả vào bảng \texttt{FormSubmission} trong cơ sở dữ liệu.
\end{enumerate}
\subsection{Thiết kế cơ sở dữ liệu}
Phần này có độ dài từ hai đến bốn trang. Sinh viên thiết kế, vẽ và giải thích biểu đồ thực thể liên kết (E-R diagram). Từ đó, sinh viên thiết kế cơ sở dữ liệu tùy theo hệ quản trị cơ sở dữ liệu mà mình sử dụng (SQL, NoSQL, Firebase, v.v.)

\section{Xây dựng ứng dụng}
\subsection{Thư viện và công cụ sử dụng}
Sinh viên liệt kê các công cụ, ngôn ngữ lập trình, API, thư viện, IDE, công cụ kiểm thử, v.v. mà mình sử dụng để phát triển ứng dụng. Mỗi công cụ phải được chỉ rõ phiên bản sử dụng. SV nên kẻ bảng mô tả tương tự như Bảng \ref{table:my_label}. Nếu có nhiều nội dung trình bày, sinh viên cần xoay ngang bảng.

\begin{longtable}{lll}
    \caption{Danh sách thư viện và công cụ sử dụng}
    \label{table:my_label}\\
        \hline
        \textbf{Mục đích} & \textbf{Công cụ}       & \textbf{Địa chỉ URL}    \\ \hline
        IDE lập trình     & Eclipse Oxygen a64 bit & http://www.eclipse.org/ \\ \hline
        v.v.              & v.v.                   & v.v.                    \\ \hline
\end{longtable}

\subsection{Kết quả đạt được}
Sinh viên trước tiên mô tả kết quả đạt được của mình là gì, ví dụ như các sản phẩm được đóng gói là gì, bao gồm những thành phần nào, ý nghĩa, vai trò?

Sinh viên cần thống kê các thông tin về ứng dụng của mình như: số dòng code, số lớp, số gói, dung lượng toàn bộ mã nguồn, dung lượng của từng sản phẩm đóng gói, v.v. Tương tự như phần liệt kê về công cụ sử dụng, sinh viên cũng nên dùng bảng để mô tả phần thông tin thống kê này.

\subsection{Minh họa các chức năng chính}
Sinh viên lựa chọn và đưa ra màn hình cho các chức năng chính, quan trọng, và thú vị nhất. Mỗi giao diện cần phải có lời giải thích ngắn gọn. Khi giải thích, sinh viên có thể kết hợp với các chú thích ở trong hình ảnh giao diện.

\section{Kiểm thử}
Phần này có độ dài từ hai đến ba trang. Sinh viên thiết kế các trường hợp kiểm thử cho hai đến ba chức năng quan trọng nhất. Sinh viên cần chỉ rõ các kỹ thuật kiểm thử đã sử dụng. Chi tiết các trường hợp kiểm thử khác, nếu muốn trình bày, sinh viên đưa vào phần phụ lục.
Sinh viên sau cùng tổng kết về số lượng các trường hợp kiểm thử và kết quả kiểm thử. Sinh viên cần phân tích lý do nếu kết quả kiểm thử không đạt.
\section{Triển khai}
Sinh viên trình bày mô hình và/hoặc cách thức triển khai thử nghiệm/thực tế. Ứng dụng của sinh viên được triển khai trên server/thiết bị gì, cấu hình như thế nào. Kết quả triển khai thử nghiệm nếu có (số lượng người dùng, số lượng truy cập, thời gian phản hồi, phản hồi người dùng, khả năng chịu tải, các thống kê, v.v.)

\end{document}
